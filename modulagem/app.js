const greeting = require('./greeting.js') //carrega um módulo para você

const name = 'cliente'
greeting(name)

//separete the code in to modules in order to make the code reusable and much better strutured :)